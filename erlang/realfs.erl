-module(realfs).
-compile(export_all).

openr( NameFile) -> error("No implementado").
openw( NameFile) -> error("No implementado").
read ( Handle, Size) -> error("No implementado").
write ( Handle, Txt) -> error("No implementado").
